{
  "name": "tl-skills",
  "version": "0.1.0",
  "description": "CLI for the TwelveLabs skills marketplace",
  "bin": {
    "tl-skills": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --sourcemap",
    "dev": "bun run src/index.ts",
    "prepare": "bun run build"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
